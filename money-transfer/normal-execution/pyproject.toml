[project]
name = "banking-normal-execution"
version = "1.0.0"
description = "Illustrates the inherent volatility of a normal execution and how a crash negatively affects application state"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
