[project]
name = "banking-durable-execution"
version = "1.0.0"
description = "The Durable Execution (Temporal) version of the money transfer, contrasting with normal-execution"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "temporalio",
]
